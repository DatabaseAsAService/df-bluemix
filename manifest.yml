---
declared-services:
  df2-mysql:
    label: mysql
    plan: 100
applications:
- name: df2
  memory: 1024M
  buildpack: https://github.com/cloudfoundry/php-buildpack
  env:
    CF_STAGING_TIMEOUT: 15
    CF_STARTUP_TIMEOUT: 15
    BP_DEBUG: "True"
  services:
   - df2-mysql
